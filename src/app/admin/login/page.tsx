import { AuthPage } from "@/components/admin/AuthPage";

export default function LoginPage() {
  return (
    <main className="w-full min-h-screen bg-[#030014] overflow-hidden animate-in fade-in duration-500">
      <AuthPage />
    </main>
  );
}